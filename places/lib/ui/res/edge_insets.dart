import 'package:flutter/material.dart';

const cardPadding = EdgeInsets.all(16);
const listPadding = EdgeInsets.only(left: 16, top: 8, right: 16, bottom: 16);

const appBarPadding =
    EdgeInsets.only(left: 16, top: 40, right: 16, bottom: 8);
const detailsPadding =
    EdgeInsets.only(left: 16, top: 24, right: 16, bottom: 16);
